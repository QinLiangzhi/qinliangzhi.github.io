# 中兽医笔记站 · 上手步骤（零代码 / 纯网页版）

> 面向命令行零基础。全程在 github.com 网页上操作，不需要装任何软件、不需要敲命令。
> 预计用时：20–30 分钟。做完你就拥有人生第一个网站。

---

## 第 1 步：新建仓库

1. 打开 https://github.com 并登录你的账号。
2. 右上角点 **＋** → 选 **New repository（新建仓库）**。
3. 填写：
   - **Repository name（仓库名）**：`tcm-notes`（用小写英文，别用空格）
   - **Description（描述）**：中兽医学习笔记站
   - 可见性选 **Public（公开）**——GitHub Pages 免费托管要求公开仓库。
   - ✅ 勾选 **Add a README file**（先勾上，待会儿我们替换它）
4. 点 **Create repository（创建仓库）**。

## 第 2 步：把骨架文件传上去

我们已经帮你准备好 4 类文件，你要做的只是把它们"拖"进仓库：

- `index.html`（网站入口，加载 Docsify）
- `README.md`（首页内容）
- `_sidebar.md`（左侧目录）
- `notes/`（示例笔记文件夹，里面 2 篇示例）

**网页上传法（推荐新手）：**

1. 进入刚建好的仓库页面。
2. 点仓库里的 **Add file** → **Upload files**。
3. 把上面这 4 个文件/文件夹**直接拖到上传区**（文件夹也能拖）。
4. 在下方 **Commit changes** 写一句说明，例如：`初始化中兽医笔记站骨架`。
5. 点 **Commit changes** 提交。

> 提示：`README.md` 你已经勾选创建了，上传时会提示"是否覆盖"——选覆盖即可。

## 第 3 步：开启免费网站（GitHub Pages）

1. 在仓库页面点上方 **Settings（设置）**。
2. 左侧找 **Pages**（有的版本在 "Code and automation" 分组下）。
3. **Build and deployment → Branch** 选 **main**，**folder** 选 **/ (root)**。
4. 点 **Save（保存）**。
5. 页面会提示 "Your site is live at https://你的用户名.github.io/tcm-notes"。
   - 第一次通常等 **1–2 分钟**才生效，耐心一下，刷新即可。

## 第 4 步：验证成功

1. 打开 `https://你的用户名.github.io/tcm-notes`。
2. 应该看到：左侧有目录、右侧有"中兽医学习笔记"首页。
3. 点左侧「基础 → 什么是中兽医」，能打开示例笔记 = 成功 🎉

---

## 日常怎么加笔记（阶段 2 用）

1. 仓库里进 `notes/` 文件夹 → **Add file** → **Create new file**。
2. 文件名写 `你的笔记名.md`（中文也行，但建议英文/拼音更稳）。
3. 内容用 Markdown 写，第一行写 `# 标题`。
4. 提交后，去 `_sidebar.md` 点铅笔图标编辑，在对应分组下加一行：
   `* [笔记显示名](notes/你的笔记名.md)`
5. 提交，刷新网站即可看到新笔记。

---

## 卡住了怎么办

- **网站打不开 / 空白**：等 2 分钟再刷新；确认 Pages 的 Branch 是 `main` 且 folder 是 `/ (root)`。
- **左侧目录不显示**：确认仓库根目录有 `_sidebar.md`，且 `index.html` 里 `loadSidebar: true`。
- **笔记点了 404**：检查 `_sidebar.md` 里的链接路径是否和文件名一致（含中文要完全一致）。

把具体报错截图或文字发给我，我帮你定点排障。
