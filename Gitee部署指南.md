# 中兽医笔记站 · 部署到码云 Gitee（无需加速器）

> 适用场景：GitHub 在国内访问不稳定、手头没有通用代理。
> 全程在 gitee.com 网页操作，国内直连可用，不需要装软件、不需要加速器。
> 站点已改为"本地自包含"版本（Docsify 脚本在 vendor/ 里），不再依赖任何被墙的 CDN。

## 第 0 步：准备（一次性）
1. 打开 https://gitee.com ，用手机号注册 / 登录。
2. 首次开启 Gitee Pages 需要实名：右上角头像 → 设置 → 实名认证 → 绑定手机号。
   （不绑定无法开启 Pages，先做完。）

## 第 1 步：新建仓库
1. 右上角 ＋ → 新建仓库。
2. 填写：
   - 仓库名：`tcm-notes`（小写英文）
   - 是否开源：选 **公开**
   - **不要**勾选"初始化仓库"（README / .gitignore 都别勾），避免和我们自己的文件冲突
3. 点 创建。

## 第 2 步：把本地文件传上去
本地文件夹（要传的内容）：
```
D:\WB001\2026-09-13-21-10-34\tcm-notes-site
├─ index.html
├─ README.md
├─ _sidebar.md
├─ vendor\          ← 整个文件夹，含 docsify.min.js / vue.css / search.min.js
└─ notes\           ← 整个文件夹，含两篇示例笔记
```

Gitee 上传：
1. 进入刚建的仓库 → 文件 → 上传文件（或直接把文件拖到仓库页面）。
2. 把上面 **5 项一起拖进去**（文件夹也能拖）。
3. 提交说明写：`初始化中兽医笔记站`。
4. 提交。

## 第 3 步：开启 Gitee Pages
1. 仓库页面上方找 **服务** → **Gitee Pages**。
2. 部署分支：选仓库现在用的分支（一般是 `master`）。
3. 部署目录：选 **/（根目录）**。
4. （可选）勾选"强制使用 HTTPS"。
5. 点 **启动**。
6. 等约 1 分钟，页面给出网址：`https://你的Gitee用户名.gitee.io`

## 第 4 步：验证
打开 `https://你的Gitee用户名.gitee.io` ，看到左侧目录 + "中兽医学习笔记"首页 = 成功。

## 常见问题
- **打不开 / 空白**：等 1–2 分钟再刷新；确认 Gitee Pages 已"启动"、部署目录是根目录。
- **提示未实名**：去 设置 → 实名认证 绑手机。
- **左侧目录不显示**：确认仓库根目录有 `_sidebar.md`，且 `index.html` 里 `loadSidebar: true`（已是）。
- **笔记 404**：检查 `_sidebar.md` 里的链接文件名与 `notes/` 里是否完全一致（含中文）。

## 之后怎么加笔记（日常）
仓库里进 `notes/` → 新建文件 → 写 `笔记名.md` → 提交；
再去 `_sidebar.md` 编辑，加一行 `* [显示名](notes/笔记名.md)` → 提交，刷新即见。
